// TEMP demo (recording) layout switch.
//
// When true, the Run page's panels are simplified to a three-column layout:
//   left   = 多轮对话 (conversation) only
//   middle = 智能体思考过程 (thinking) only
//   right  = 运行日志 (run log / execution records, no sandbox command input)
// and the other views (原始响应 / 测试评审 / 样例编写 / 报告生成) are hidden.
//
// Set to false to restore the full tabbed panels — no other change needed.
export const DEMO_THINKING_LAYOUT = true;
